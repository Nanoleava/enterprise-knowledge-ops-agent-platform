<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ljl.agent.mapper.UserMapper">

    <resultMap id="UserResultMap"
               type="com.ljl.agent.entity.User">
        <id column="id" property="id"/>

        <result column="username"
                property="username"/>

        <result column="password_hash"
                property="passwordHash"/>

        <result column="email"
                property="email"/>

        <result column="role"
                property="role"/>

        <result column="status"
                property="status"/>

        <result column="created_at"
                property="createdAt"/>

        <result column="updated_at"
                property="updatedAt"/>
    </resultMap>

    <sql id="UserColumnList">
        id,
        username,
        password_hash,
        email,
        role,
        status,
        created_at,
        updated_at
    </sql>

    <insert id="insert"
            parameterType="com.ljl.agent.entity.User"
            useGeneratedKeys="true"
            keyProperty="id"
            keyColumn="id">
        INSERT INTO `user`
        (
        username,
        password_hash,
        email,
        role,
        status
        )
        VALUES
        (
        #{username},
        #{passwordHash},
        #{email},
        #{role},
        #{status}
        )
    </insert>

    <select id="selectById"
            resultMap="UserResultMap">
        SELECT
        <include refid="UserColumnList"/>
        FROM `user`
        WHERE id = #{id}
    </select>

    <select id="selectByUsername"
            resultMap="UserResultMap">
        SELECT
        <include refid="UserColumnList"/>
        FROM `user`
        WHERE username = #{username}
        LIMIT 1
    </select>

    <select id="selectByEmail"
            resultMap="UserResultMap">
        SELECT
        <include refid="UserColumnList"/>
        FROM `user`
        WHERE email = #{email}
        LIMIT 1
    </select>

    <select id="selectAll"
            resultMap="UserResultMap">
        SELECT
        <include refid="UserColumnList"/>
        FROM `user`
        ORDER BY id ASC
    </select>

</mapper>